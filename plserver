#/bin/bash
servername="myserver"
echo $servername

docker ps -a | grep myserver | awk '{print $1}' | xargs docker stop
docker ps -a | grep myserver | awk '{print $1}' | xargs docker rm

docker run -d --name $servername -v "$PWD/src":/tmp/plhw -w /tmp/plhw yyuzhong/pvamucs:PL $@
